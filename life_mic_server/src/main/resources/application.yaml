server:
  port: 8080

spring:
  application:
    name: eureka_server

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://localhost:8080/eureka

logging:
  level:
    com:
      netflix:
        eureka: OFF
        discovery: OFF
